const sys=[];
var i;
for(i=70;i<151;i++){
   var obj={label:i,value:i}
   sys.push(obj);
}

const dia=[];
var i;
for(i=10;i<151;i++){
   var obj={label:i,value:i}
   dia.push(obj);
}

//clsconsole.log(ages);cls
export default sys;
